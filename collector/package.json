{
  "name": "fabric-capacity-manager-audit-collector",
  "version": "1.0.0",
  "description": "Azure Functions collector that authenticates and persists Fabric Capacity Manager admin audit events to Azure Table Storage and Application Insights.",
  "license": "MIT",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@azure/data-tables": "^13.3.0",
    "@azure/functions": "^4.7.0",
    "@azure/identity": "^4.5.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0"
  }
}
